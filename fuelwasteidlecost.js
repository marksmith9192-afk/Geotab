var fuelWasteDashboard=(function(){
  var api=null,loaded=false,isLoading=false,DEBUG=true,allVehicles=[],filteredVehicles=[],vehicleMap={},deviceById={},activeReviewId=null;
  var DAYS=30,BATCH_SIZE=12,DEVICE_CAP=120,LITERS_PER_GALLON=3.785411784,KG_PER_GAL={gasoline:8.887,diesel:10.180};
  var SETTINGS_KEY='traxxisFuelWasteSettings';
  var settings={fuelType:'diesel',fuelPrice:4.75,priceUnit:'gallon',idleBurnRateLph:3.5};
  function byId(id){return document.getElementById(id)}
  function esc(v){return String(v).replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;').replace(/"/g,'&quot;').replace(/'/g,'&#39;')}
  function fmtD(d){return d?new Date(d).toLocaleString():'Unknown'}
  function daysAgo(days){var d=new Date();d.setDate(d.getDate()-days);return d}
  function toNum(v){var n=Number(v);return isNaN(n)?0:n}
  function sum(arr,fn){var i,total=0;for(i=0;i<arr.length;i++)total+=fn?fn(arr[i]):arr[i];return total}
  function setDebug(msg){var el=byId('debugBox');if(!DEBUG){el.className='debug hidden';return;}el.className='debug';el.textContent=msg}
  function setStatus(msg,helper,retry){byId('statusMessage').textContent=msg;byId('statusHelper').textContent=helper||'';byId('retryButton').className=retry?'btn btnSecondary':'btn btnSecondary hidden'}
  function setLoading(v){isLoading=v;byId('refreshButton').disabled=v;byId('retryButton').disabled=v;byId('exportButton').disabled=v;byId('applySettingsButton').disabled=v}
  function litersToGallons(l){return toNum(l)/LITERS_PER_GALLON}
  function fuelPricePerLiter(){return settings.priceUnit==='liter'?settings.fuelPrice:(settings.fuelPrice/LITERS_PER_GALLON)}
  function co2KgFromLiters(liters){return litersToGallons(liters)*KG_PER_GAL[settings.fuelType]}
  function fmtLiters(v){return Math.round(toNum(v)).toLocaleString()+' L'}
  function fmtHours(v){return toNum(v).toFixed(1)+' h'}
  function fmtCurrency(v){return new Intl.NumberFormat(undefined,{style:'currency',currency:'USD',maximumFractionDigits:0}).format(toNum(v))}
  function fmtPercent(v){return Math.round(toNum(v)*100)+'%'}
  function fmtKg(v){var n=toNum(v);return n>=1000?(n/1000).toFixed(1)+' t':Math.round(n).toLocaleString()+' kg'}
  function durationToHours(value){if(value===null||value===undefined)return 0;if(typeof value==='number'){if(value>86400000)return value/3600000;if(value>10000)return value/1000/3600;return value/3600;}if(typeof value==='string'){var iso=value.match(/P(?:\d+D)?T(?:(\d+)H)?(?:(\d+)M)?(?:(\d+(?:\.\d+)?)S)?/i);if(iso)return toNum(iso[1])+toNum(iso[2])/60+toNum(iso[3])/3600;var parts=value.split(':');if(parts.length===3)return toNum(parts[0])+toNum(parts[1])/60+toNum(parts[2])/3600;var numeric=Number(value);if(!isNaN(numeric))return durationToHours(numeric);}if(typeof value==='object'){if(value.ticks!==undefined)return toNum(value.ticks)/10000000/3600;if(value.TotalHours!==undefined)return toNum(value.TotalHours);if(value.totalHours!==undefined)return toNum(value.totalHours);if(value.hours!==undefined||value.minutes!==undefined||value.seconds!==undefined)return toNum(value.hours)+toNum(value.minutes)/60+toNum(value.seconds)/3600;}return 0;}
  function groupNameFromRef(ref,map){var id,name;if(!ref)return'';if(typeof ref==='string')return map[ref]||ref;id=ref.id||'';name=ref.name||'';return map[id]||name||id||''}
  function isBuiltInGroupName(name){return !name||/^Group[A-Z]/.test(name)||/^b[0-9A-F]+$/i.test(name)||name==='Company group'||name==='Asset Information'||name==='Driver activity'}
  function resolveDeviceGroupName(device,groupMap){var groups=device.groups||[],i,name,fallback='';for(i=0;i<groups.length;i++){name=groupNameFromRef(groups[i],groupMap);if(name&&!isBuiltInGroupName(name))return name;if(name&&!fallback)fallback=name;}return fallback&&!isBuiltInGroupName(fallback)?fallback:'Ungrouped'}
  function offenderClass(row){if(row.idleCost>=400||row.idleShare>=0.35)return'high';if(row.idleCost>=150||row.idleShare>=0.18)return'medium';return'low'}
  function sevChip(level){if(level==='high')return '<span class="chip chipCritical">High idle cost</span>';if(level==='medium')return '<span class="chip chipWarning">Watch list</span>';return '<span class="chip chipHealthy">Lower waste</span>'}
  function trendClass(row){if(row.weeklyIdleDelta>50||row.weeklyFuelDelta>150)return'trendUp';if(row.weeklyIdleDelta>0||row.idleShare>=0.18)return'trendWatch';return'trendStable'}
  function trendText(row){if(row.weeklyIdleDelta>50)return'Idle cost rising';if(row.weeklyFuelDelta>150)return'Fuel spend climbing';if(row.idleShare>=0.18)return'Idle share elevated';return'Stable'}
  function reviewButton(id){return '<button class="reviewBtn" type="button" data-review="'+esc(id)+'">Open Review</button>'}
  function runBatch(calls,start,done,fail,accum){var batch=calls.slice(start,start+BATCH_SIZE);if(!batch.length){done(accum);return;}api.multiCall(batch,function(results){runBatch(calls,start+BATCH_SIZE,done,fail,accum.concat(results||[]));},function(e){fail(e);});}
  function errText(e){if(!e)return'Unknown error';if(typeof e==='string')return e;if(e.message)return e.message;try{return JSON.stringify(e)}catch(x){return String(e)}}
  function loadGroups(done,fail){api.call('Get',{typeName:'Group',search:{},resultsLimit:5000},function(groups){var map={},i;groups=groups||[];for(i=0;i<groups.length;i++)if(groups[i]&&groups[i].id)map[groups[i].id]=groups[i].name||groups[i].id;done(map);},function(e){if(typeof fail==='function')fail(e);});}
  function createDayMap(){var map={},order=[],i,d,key;for(i=DAYS-1;i>=0;i--){d=daysAgo(i);key=d.getFullYear()+'-'+(d.getMonth()+1)+'-'+d.getDate();map[key]={key:key,label:d.toLocaleDateString(undefined,{month:'short',day:'numeric'}),fuelLiters:0,idleFuelLiters:0,idleHours:0,cost:0,idleCost:0,co2Kg:0};order.push(key);}return{map:map,order:order};}
  function keyForDate(dateValue){var d=new Date(dateValue);return d.getFullYear()+'-'+(d.getMonth()+1)+'-'+d.getDate()}
  function buildWeeklyBuckets(dayMap){var keys=dayMap.order,buckets=[],i,start,end,label,bucket,j;for(i=0;i<keys.length;i+=7){start=i;end=Math.min(i+7,keys.length)-1;label=dayMap.map[keys[start]].label+' - '+dayMap.map[keys[end]].label;bucket={label:label,fuelLiters:0,idleFuelLiters:0,idleHours:0,cost:0,idleCost:0,co2Kg:0};for(j=start;j<=end;j++){bucket.fuelLiters+=dayMap.map[keys[j]].fuelLiters;bucket.idleFuelLiters+=dayMap.map[keys[j]].idleFuelLiters;bucket.idleHours+=dayMap.map[keys[j]].idleHours;bucket.cost+=dayMap.map[keys[j]].cost;bucket.idleCost+=dayMap.map[keys[j]].idleCost;bucket.co2Kg+=dayMap.map[keys[j]].co2Kg;}buckets.push(bucket);}return buckets;}
  function calculateDailyCosts(dayMap){var pricePerLiter=fuelPricePerLiter(),keys=dayMap.order,i,day;for(i=0;i<keys.length;i++){day=dayMap.map[keys[i]];day.cost=day.fuelLiters*pricePerLiter;day.idleCost=day.idleFuelLiters*pricePerLiter;day.co2Kg=co2KgFromLiters(day.fuelLiters);}return dayMap;}
  function analyzeVehicle(device,fuelRows,trips){var dayMap=createDayMap(),i,key,row,totalFuelLiters=0,totalIdleFuelLiters=0,totalIdleHours=0,totalTrips=0,actualIdleFuelSeen=false;fuelRows=fuelRows||[];trips=trips||[];for(i=0;i<fuelRows.length;i++){row=fuelRows[i]||{};key=keyForDate(row.dateTime||row.DateTime);if(!dayMap.map[key])continue;dayMap.map[key].fuelLiters+=toNum(row.totalFuelUsed!==undefined?row.totalFuelUsed:row.TotalFuelUsed);if(row.totalIdlingFuelUsedL!==undefined&&row.totalIdlingFuelUsedL!==null){dayMap.map[key].idleFuelLiters+=toNum(row.totalIdlingFuelUsedL);actualIdleFuelSeen=true;}else if(row.TotalIdlingFuelUsedL!==undefined&&row.TotalIdlingFuelUsedL!==null){dayMap.map[key].idleFuelLiters+=toNum(row.TotalIdlingFuelUsedL);actualIdleFuelSeen=true;}}for(i=0;i<trips.length;i++){row=trips[i]||{};key=keyForDate(row.stop||row.Stop||row.start||row.Start);if(!dayMap.map[key])continue;dayMap.map[key].idleHours+=durationToHours(row.idlingDuration!==undefined?row.idlingDuration:row.IdlingDuration);totalTrips+=1;}for(i=0;i<dayMap.order.length;i++){key=dayMap.order[i];if(!actualIdleFuelSeen&&dayMap.map[key].idleHours>0)dayMap.map[key].idleFuelLiters=dayMap.map[key].idleHours*settings.idleBurnRateLph;}calculateDailyCosts(dayMap);for(i=0;i<dayMap.order.length;i++){key=dayMap.order[i];row=dayMap.map[key];totalFuelLiters+=row.fuelLiters;totalIdleFuelLiters+=row.idleFuelLiters;totalIdleHours+=row.idleHours;}var totalFuelCost=totalFuelLiters*fuelPricePerLiter();var totalIdleCost=totalIdleFuelLiters*fuelPricePerLiter();var weekly=buildWeeklyBuckets(dayMap);var previous=weekly.length>1?weekly[weekly.length-2]:null;var latest=weekly.length?weekly[weekly.length-1]:null;var idleShare=totalFuelLiters>0?totalIdleFuelLiters/totalFuelLiters:0;var offender=offenderClass({idleCost:totalIdleCost,idleShare:idleShare});var recommendation='Keep monitoring current fuel and idle patterns.';if(offender==='high')recommendation='Review route, PTO, and idle policy on this vehicle first.';else if(offender==='medium')recommendation='Coach idle behavior and confirm site dwell expectations.';return{deviceId:device.id,name:device.name||device.serialNumber||'Unnamed vehicle',groupName:device.primaryGroupName||'Ungrouped',fuelLiters:totalFuelLiters,fuelCost:totalFuelCost,idleHours:totalIdleHours,idleFuelLiters:totalIdleFuelLiters,idleCost:totalIdleCost,co2Kg:co2KgFromLiters(totalFuelLiters),idleCo2Kg:co2KgFromLiters(totalIdleFuelLiters),idleShare:idleShare,weeklyFuelDelta:latest&&previous?(latest.cost-previous.cost):0,weeklyIdleDelta:latest&&previous?(latest.idleCost-previous.idleCost):0,offenderLevel:offender,recommendation:recommendation,totalTrips:totalTrips,usedActualIdleFuel:actualIdleFuelSeen,dailyMap:dayMap,weekly:weekly,_fuelRows:fuelRows,_tripRows:trips};}
  function hydrateVehicleMap(){var i;vehicleMap={};for(i=0;i<allVehicles.length;i++)vehicleMap[allVehicles[i].deviceId]=allVehicles[i]}
  function updateSettingsSummary(){byId('settingsSummary').textContent='Using '+settings.fuelType+' at '+fmtCurrency(settings.fuelPrice)+' per '+(settings.priceUnit==='gallon'?'gallon':'liter')+' and '+settings.idleBurnRateLph.toFixed(1)+' L/hour as the fallback idle burn rate.'}
  function loadSettings(){try{var raw=localStorage.getItem(SETTINGS_KEY),saved=raw?JSON.parse(raw):null;if(saved){settings.fuelType=saved.fuelType||settings.fuelType;settings.fuelPrice=toNum(saved.fuelPrice)||settings.fuelPrice;settings.priceUnit=saved.priceUnit||settings.priceUnit;settings.idleBurnRateLph=toNum(saved.idleBurnRateLph)||settings.idleBurnRateLph;}}catch(e){}byId('fuelTypeInput').value=settings.fuelType;byId('fuelPriceInput').value=settings.fuelPrice;byId('priceUnitInput').value=settings.priceUnit;byId('idleBurnRateInput').value=settings.idleBurnRateLph;updateSettingsSummary();}
  function saveSettings(){settings.fuelType=byId('fuelTypeInput').value;settings.fuelPrice=toNum(byId('fuelPriceInput').value)||0;settings.priceUnit=byId('priceUnitInput').value;settings.idleBurnRateLph=toNum(byId('idleBurnRateInput').value)||0;localStorage.setItem(SETTINGS_KEY,JSON.stringify(settings));updateSettingsSummary();}
  function recomputeAllVehicles(){var i,updated=[];for(i=0;i<allVehicles.length;i++)updated.push(analyzeVehicle(deviceById[allVehicles[i].deviceId]||{id:allVehicles[i].deviceId,name:allVehicles[i].name,primaryGroupName:allVehicles[i].groupName},allVehicles[i]._fuelRows,allVehicles[i]._tripRows));updated.sort(function(a,b){if(b.idleCost!==a.idleCost)return b.idleCost-a.idleCost;return b.idleHours-a.idleHours});allVehicles=updated;hydrateVehicleMap();applyFilters();if(activeReviewId&&vehicleMap[activeReviewId])openReview(activeReviewId);}
  function buildGroupOptions(devices){var select=byId('groupFilter'),names={},i,name,options=['<option value="__all__">All groups</option>'],current=select.value||'__all__';for(i=0;i<devices.length;i++){name=devices[i].primaryGroupName||'';if(name&&name!=='Ungrouped')names[name]=1;}var keys=Object.keys(names).sort();for(i=0;i<keys.length;i++)options.push('<option value="'+esc(keys[i])+'">'+esc(keys[i])+'</option>');select.innerHTML=options.join('');if(current&&current!=='__all__'&&names[current])select.value=current;else select.value='__all__';}
  function refreshResolvedGroups(devices){loadGroups(function(groupMap){var i,id,row,resolved,updated=0;for(i=0;i<devices.length;i++){resolved=resolveDeviceGroupName(devices[i],groupMap);if(resolved&&resolved!==devices[i].primaryGroupName){devices[i].primaryGroupName=resolved;updated+=1;}id=devices[i].id;row=vehicleMap[id];if(row&&resolved&&row.groupName!==resolved)row.groupName=resolved;}buildGroupOptions(devices);applyFilters();setDebug(byId('debugBox').textContent+'\nResolved custom groups: '+updated);},function(e){setDebug(byId('debugBox').textContent+'\nGroup name lookup skipped: '+errText(e));});}
  function renderFleetSummary(rows){var fuel=sum(rows,function(r){return r.fuelLiters}),cost=sum(rows,function(r){return r.fuelCost}),idleCost=sum(rows,function(r){return r.idleCost}),co2=sum(rows,function(r){return r.co2Kg}),idleHours=sum(rows,function(r){return r.idleHours});byId('fleetFuelUsed').textContent=fmtLiters(fuel);byId('fleetFuelCost').textContent=fmtCurrency(cost);byId('fleetIdleCost').textContent=fmtCurrency(idleCost);byId('fleetCo2').textContent=fmtKg(co2);byId('fleetFuelSub').textContent=fmtHours(idleHours)+' idle across visible vehicles';byId('fleetCostSub').textContent='Using '+fmtCurrency(settings.fuelPrice)+' / '+(settings.priceUnit==='gallon'?'gal':'L');byId('fleetIdleSub').textContent=rows.length+' vehicles in current view';byId('fleetCo2Sub').textContent=settings.fuelType==='diesel'?'Diesel tailpipe estimate':'Gasoline tailpipe estimate';}
  function renderTrendChart(rows,targetId){var agg=createDayMap(),i,j,key,buckets,w=980,h=260,p=34,maxBar=0,maxLine=0,barW,x,fuelHeight,idleHeight,costY,co2Y,svgBars='',svgLines='',labels='',grid='';for(i=0;i<rows.length;i++){for(j=0;j<rows[i].dailyMap.order.length;j++){key=rows[i].dailyMap.order[j];agg.map[key].fuelLiters+=rows[i].dailyMap.map[key].fuelLiters;agg.map[key].idleFuelLiters+=rows[i].dailyMap.map[key].idleFuelLiters;agg.map[key].idleHours+=rows[i].dailyMap.map[key].idleHours;}}calculateDailyCosts(agg);buckets=buildWeeklyBuckets(agg);if(!buckets.length){byId(targetId).innerHTML='<div class="sub">No weekly trend data is available yet.</div>';return;}for(i=0;i<buckets.length;i++){if(buckets[i].cost>maxBar)maxBar=buckets[i].cost;if(buckets[i].co2Kg>maxLine)maxLine=buckets[i].co2Kg;}if(maxBar<=0)maxBar=1;if(maxLine<=0)maxLine=1;barW=(w-p*2)/buckets.length;function barY(v,max){return h-p-((v/max)*(h-p*2))}function lineY(v){return h-p-((v/maxLine)*(h-p*2))}for(i=0;i<4;i++){var gy=p+i*((h-p*2)/3);grid+='<line class="gridLine" x1="'+p+'" y1="'+gy+'" x2="'+(w-p)+'" y2="'+gy+'" />';}var costPoints='',co2Points='';for(i=0;i<buckets.length;i++){x=p+i*barW;fuelHeight=((buckets[i].cost/maxBar)*(h-p*2));idleHeight=((buckets[i].idleCost/maxBar)*(h-p*2));svgBars+='<rect class="barFuel" x="'+(x+6).toFixed(1)+'" y="'+(h-p-fuelHeight).toFixed(1)+'" width="'+Math.max(16,barW-12).toFixed(1)+'" height="'+fuelHeight.toFixed(1)+'" rx="4" />';svgBars+='<rect class="barIdle" x="'+(x+10).toFixed(1)+'" y="'+(h-p-idleHeight).toFixed(1)+'" width="'+Math.max(8,barW-20).toFixed(1)+'" height="'+idleHeight.toFixed(1)+'" rx="4" />';costY=barY(buckets[i].idleCost,maxBar);co2Y=lineY(buckets[i].co2Kg);costPoints+=(i?' ':'')+(x+barW/2).toFixed(1)+','+costY.toFixed(1);co2Points+=(i?' ':'')+(x+barW/2).toFixed(1)+','+co2Y.toFixed(1);labels+='<text class="axisText" x="'+(x+4).toFixed(1)+'" y="'+(h-8)+'">'+esc(buckets[i].label)+'</text>';}svgLines='<polyline class="seriesLine" points="'+costPoints+'" /><polyline class="seriesLineMuted" points="'+co2Points+'" />';byId(targetId).innerHTML='<svg class="chart" viewBox="0 0 '+w+' '+h+'" role="img" aria-label="Weekly fuel and idle cost chart">'+grid+svgBars+svgLines+labels+'<text class="axisText" x="4" y="18">Fuel cost bars</text><text class="axisText" x="4" y="34">Idle cost line and CO2 line</text><text class="axisText" x="'+(w-p-150)+'" y="20">Blue line: idle cost</text><text class="axisText" x="'+(w-p-150)+'" y="36">Green line: CO2</text></svg>';}
  function render(rows){var i,h='',trendNote='';if(!rows.length){byId('vehicleTableBody').innerHTML='<tr><td colspan="11" class="empty">No vehicles match the current filters.</td></tr>';return;}for(i=0;i<rows.length;i++){trendNote='<div class="sub">Fuel delta vs prior week: '+fmtCurrency(rows[i].weeklyFuelDelta)+'</div>';h+='<tr><td><button class="vehicleButton" type="button" data-review="'+esc(rows[i].deviceId)+'"><div class="vehicle">'+esc(rows[i].name)+'</div><div class="sub">Group: '+esc(rows[i].groupName||'Ungrouped')+'</div><div class="sub">'+(rows[i].usedActualIdleFuel?'Idle fuel from Geotab fuel records':'Idle fuel estimated from idle hours')+'</div></button></td><td>'+sevChip(rows[i].offenderLevel)+'</td><td>'+esc(fmtLiters(rows[i].fuelLiters))+'</td><td>'+esc(fmtCurrency(rows[i].fuelCost))+'</td><td>'+esc(fmtHours(rows[i].idleHours))+'</td><td>'+esc(fmtLiters(rows[i].idleFuelLiters))+'</td><td>'+esc(fmtCurrency(rows[i].idleCost))+'</td><td>'+esc(fmtKg(rows[i].co2Kg))+'</td><td>'+esc(fmtPercent(rows[i].idleShare))+'<div class="sub">of total fuel used</div></td><td><span class="'+trendClass(rows[i])+'">'+trendText(rows[i])+'</span><div class="sub">Idle delta vs prior week: '+fmtCurrency(rows[i].weeklyIdleDelta)+'</div>'+trendNote+'</td><td>'+reviewButton(rows[i].deviceId)+'</td></tr>';}byId('vehicleTableBody').innerHTML=h}
  function applyFilters(){var group=byId('groupFilter').value,status=byId('statusFilter').value,q=(byId('searchFilter').value||'').toLowerCase(),out=[],i,row;for(i=0;i<allVehicles.length;i++){row=allVehicles[i];if(group!=='__all__'&&row.groupName!==group)continue;if(status!=='__all__'&&row.offenderLevel!==status)continue;if(q&&row.name.toLowerCase().indexOf(q)===-1)continue;out.push(row);}filteredVehicles=out;renderFleetSummary(out);renderTrendChart(out,'fleetTrendChart');render(out)}
  function buildNarrative(row){var items=[];items.push('This vehicle used '+fmtLiters(row.fuelLiters)+' over the last 30 days and generated '+fmtCurrency(row.fuelCost)+' in modeled fuel spend.');if(row.idleShare>=0.35)items.push('Idle fuel is consuming '+fmtPercent(row.idleShare)+' of total fuel used, which is high enough to prioritize coaching or workflow review.');else if(row.idleShare>=0.18)items.push('Idle share is elevated at '+fmtPercent(row.idleShare)+', which is worth watching against route and dwell expectations.');else items.push('Idle share remains relatively contained at '+fmtPercent(row.idleShare)+' of total fuel used.');if(row.weeklyIdleDelta>50)items.push('Idle cost increased by '+fmtCurrency(row.weeklyIdleDelta)+' compared with the prior week.');else if(row.weeklyIdleDelta<0)items.push('Idle cost improved by '+fmtCurrency(Math.abs(row.weeklyIdleDelta))+' compared with the prior week.');items.push(row.usedActualIdleFuel?'Idle fuel is based on Geotab fuel records where available.':'Idle fuel is estimated using the fallback idle burn-rate assumption.');items.push('Recommended next step: '+row.recommendation);return items}
  function buildEvents(row){var items=[],weekly=row.weekly||[],sorted=weekly.slice().sort(function(a,b){return b.idleCost-a.idleCost}),top=sorted[0],last=weekly[weekly.length-1];if(top)items.push('<li><strong>Highest idle-cost week:</strong> '+esc(top.label)+' at '+esc(fmtCurrency(top.idleCost))+'.</li>');if(last)items.push('<li><strong>Most recent week:</strong> '+esc(last.label)+' with '+esc(fmtLiters(last.fuelLiters))+' fuel used and '+esc(fmtHours(last.idleHours))+' idling.</li>');if(row.idleCo2Kg>0)items.push('<li><strong>Idle CO2 estimate:</strong> '+esc(fmtKg(row.idleCo2Kg))+' from idle-related fuel burn.</li>');items.push('<li><strong>Fuel data source:</strong> '+esc(row.usedActualIdleFuel?'actual total idling fuel records':'fuel records plus idle-hour estimate fallback')+'.</li>');return items.join('')}
  function buildSplit(row){return '<div class="noteCard"><strong>Cost focus</strong>'+esc(row.idleCost>=250?'This vehicle is creating enough idle spend to justify targeted action first.':'This vehicle is not the top cost problem today, but the idle trend is still visible.')+'</div><div class="noteCard"><strong>Operational read</strong>'+esc(row.idleHours>=20?'Idle hours are substantial, so route dwell, PTO use, and site staging should be reviewed together.':'Idle hours are moderate, so driver behavior and short-stop patterns are the first place to look.')+'</div><div class="noteCard"><strong>Data confidence</strong>'+esc(row.usedActualIdleFuel?'Idle fuel totals are grounded in Geotab fuel-used records for this period.':'Idle fuel totals rely partly or fully on the editable fallback burn-rate assumption.')+'</div>'}
  function buildVehicleTrendChart(weekly){var w=760,h=220,p=26,maxFuel=0,maxIdle=0,i,x,barW,svg='',linePoints='';if(!weekly.length)return '<div class="sub">No weekly trend data is available for this vehicle.</div>';for(i=0;i<weekly.length;i++){if(weekly[i].fuelLiters>maxFuel)maxFuel=weekly[i].fuelLiters;if(weekly[i].idleCost>maxIdle)maxIdle=weekly[i].idleCost;}if(maxFuel<=0)maxFuel=1;if(maxIdle<=0)maxIdle=1;barW=(w-p*2)/weekly.length;for(i=0;i<weekly.length;i++){var barH=((weekly[i].fuelLiters/maxFuel)*(h-p*2));x=p+i*barW;svg+='<rect class="barFuel" x="'+(x+4).toFixed(1)+'" y="'+(h-p-barH).toFixed(1)+'" width="'+Math.max(12,barW-8).toFixed(1)+'" height="'+barH.toFixed(1)+'" rx="4" />';linePoints+=(i?' ':'')+(x+barW/2).toFixed(1)+','+(h-p-((weekly[i].idleCost/maxIdle)*(h-p*2))).toFixed(1);svg+='<text class="axisText" x="'+x.toFixed(1)+'" y="'+(h-6)+'">'+esc(weekly[i].label)+'</text>';}return '<svg class="chart" viewBox="0 0 '+w+' '+h+'" role="img" aria-label="Vehicle weekly trend"><line class="gridLine" x1="'+p+'" y1="'+(h-p)+'" x2="'+(w-p)+'" y2="'+(h-p)+'" />'+svg+'<polyline class="seriesLine" points="'+linePoints+'" /></svg>'}
  function buildDailyChart(dayMap){var keys=dayMap.order,w=760,h=220,p=26,max=0,maxIdleHours=0,i,x,barW,svg='',linePoints='';if(!keys.length)return '<div class="sub">No daily data is available for this vehicle.</div>';for(i=0;i<keys.length;i++){if(dayMap.map[keys[i]].fuelLiters>max)max=dayMap.map[keys[i]].fuelLiters;if(dayMap.map[keys[i]].idleFuelLiters>max)max=dayMap.map[keys[i]].idleFuelLiters;if(dayMap.map[keys[i]].idleHours>maxIdleHours)maxIdleHours=dayMap.map[keys[i]].idleHours;}if(max<=0)max=1;if(maxIdleHours<=0)maxIdleHours=1;barW=(w-p*2)/keys.length;for(i=0;i<keys.length;i++){x=p+i*barW;var fuelH=((dayMap.map[keys[i]].fuelLiters/max)*(h-p*2));var idleH=((dayMap.map[keys[i]].idleFuelLiters/max)*(h-p*2));svg+='<rect class="barFuel" x="'+(x+1).toFixed(1)+'" y="'+(h-p-fuelH).toFixed(1)+'" width="'+Math.max(2,barW-2).toFixed(1)+'" height="'+fuelH.toFixed(1)+'" rx="2" />';svg+='<rect class="barIdle" x="'+(x+1).toFixed(1)+'" y="'+(h-p-idleH).toFixed(1)+'" width="'+Math.max(2,barW-2).toFixed(1)+'" height="'+idleH.toFixed(1)+'" rx="2" />';if(i%Math.max(1,Math.ceil(keys.length/6))===0)svg+='<text class="axisText" x="'+x.toFixed(1)+'" y="'+(h-6)+'">'+esc(dayMap.map[keys[i]].label)+'</text>';linePoints+=(i?' ':'')+(x+barW/2).toFixed(1)+','+(h-p-((dayMap.map[keys[i]].idleHours/maxIdleHours)*(h-p*2))).toFixed(1);}return '<svg class="chart" viewBox="0 0 '+w+' '+h+'" role="img" aria-label="Vehicle daily trend"><line class="gridLine" x1="'+p+'" y1="'+(h-p)+'" x2="'+(w-p)+'" y2="'+(h-p)+'" />'+svg+'<polyline class="seriesLineMuted" points="'+linePoints+'" /></svg>'}
  function openReview(id){var row=vehicleMap[id],meta=[];if(!row)return;activeReviewId=id;meta.push(sevChip(row.offenderLevel));meta.push('<span class="eventBadge">'+esc(fmtPercent(row.idleShare))+' idle share</span>');meta.push('<span class="eventBadge">'+esc(row.usedActualIdleFuel?'Actual idle fuel data':'Fallback idle estimate')+'</span>');byId('reviewTitle').textContent=row.name;byId('reviewMeta').innerHTML=meta.join('');byId('reviewCopy').textContent=row.offenderLevel==='high'?'This vehicle is one of the clearest opportunities to reduce idle waste and operating cost.':row.offenderLevel==='medium'?'This vehicle shows enough idle waste to justify a closer operating review.':'This vehicle is currently a lower-priority fuel waste case based on the last 30 days.';byId('reviewFuelUsed').textContent=fmtLiters(row.fuelLiters);byId('reviewFuelCost').textContent=fmtCurrency(row.fuelCost);byId('reviewIdleCost').textContent=fmtCurrency(row.idleCost);byId('reviewCo2').textContent=fmtKg(row.co2Kg);byId('reviewTrendChart').innerHTML=buildVehicleTrendChart(row.weekly);byId('reviewDailyChart').innerHTML=buildDailyChart(row.dailyMap);byId('reviewNarrative').innerHTML=buildNarrative(row).map(function(item){return '<li>'+esc(item)+'</li>';}).join('');byId('reviewEvents').innerHTML=buildEvents(row);byId('reviewSplit').innerHTML=buildSplit(row);byId('reviewModal').className='modalShell';byId('reviewModal').setAttribute('aria-hidden','false')}
  function closeReview(){byId('reviewModal').className='modalShell hidden';byId('reviewModal').setAttribute('aria-hidden','true')}
  function exportCsv(){var rows=filteredVehicles.length?filteredVehicles:allVehicles,i,lines=['Vehicle,Group,OffenderLevel,FuelLiters,FuelCost,IdleHours,IdleFuelLiters,IdleCost,Co2Kg,IdleShare,IdleFuelSource'];for(i=0;i<rows.length;i++){lines.push('"'+rows[i].name.replace(/"/g,'""')+'","'+String(rows[i].groupName||'').replace(/"/g,'""')+'","'+rows[i].offenderLevel+'",'+rows[i].fuelLiters.toFixed(2)+','+rows[i].fuelCost.toFixed(2)+','+rows[i].idleHours.toFixed(2)+','+rows[i].idleFuelLiters.toFixed(2)+','+rows[i].idleCost.toFixed(2)+','+rows[i].co2Kg.toFixed(2)+','+rows[i].idleShare.toFixed(4)+',"'+(rows[i].usedActualIdleFuel?'Actual':'Estimated')+'"');}var blob=new Blob([lines.join('\r\n')],{type:'text/csv'}),url=URL.createObjectURL(blob),a=document.createElement('a');a.href=url;a.download='fuel-waste-idle-cost.csv';document.body.appendChild(a);a.click();document.body.removeChild(a);URL.revokeObjectURL(url);}
  function handleReviewClick(e){var btn=e.target.closest('[data-review]');if(btn)openReview(btn.getAttribute('data-review'))}
  function run(){if(isLoading||!api)return;closeReview();setLoading(true);saveSettings();setStatus('Loading 30 days of fuel and trip data...','This view combines Geotab fuel-used events with trip idle duration to surface cost and emissions quickly.',false);setDebug('Starting fuel waste analysis...');var fromDate=daysAgo(DAYS),toDate=new Date();api.call('Get',{typeName:'Device',search:{},resultsLimit:1000},function(devices){devices=(devices||[]).slice(0,DEVICE_CAP);deviceById={};for(var d=0;d<devices.length;d++){devices[d].primaryGroupName=(devices[d].groups&&devices[d].groups.length&&(devices[d].groups[0].name||devices[d].groups[0].id))?String(devices[d].groups[0].name||devices[d].groups[0].id):'Ungrouped';deviceById[devices[d].id]=devices[d];}buildGroupOptions(devices);var fuelCalls=[],tripCalls=[],i,idx=0,tripIdx=0,vehicleRows=[];for(i=0;i<devices.length;i++){fuelCalls.push(['Get',{typeName:'FuelUsed',search:{deviceSearch:{id:devices[i].id},fromDate:fromDate,toDate:toDate},resultsLimit:5000}]);tripCalls.push(['Get',{typeName:'Trip',search:{deviceSearch:{id:devices[i].id},fromDate:fromDate,toDate:toDate},resultsLimit:5000}]);}setDebug('Devices: '+devices.length+'\nFuelUsed calls: '+fuelCalls.length+'\nTrip calls: '+tripCalls.length+'\nBatch size: '+BATCH_SIZE);if(!fuelCalls.length){allVehicles=[];hydrateVehicleMap();applyFilters();setStatus('No devices found.','No vehicles were returned by the Device query.',true);setLoading(false);return;}runBatch(fuelCalls,0,function(fuelResults){runBatch(tripCalls,0,function(tripResults){for(i=0;i<devices.length;i++){var analyzed=analyzeVehicle(devices[i],fuelResults[idx]||[],tripResults[tripIdx]||[]);idx+=1;tripIdx+=1;if(analyzed.fuelLiters>0||analyzed.idleHours>0)vehicleRows.push(analyzed);}vehicleRows.sort(function(a,b){if(b.idleCost!==a.idleCost)return b.idleCost-a.idleCost;return b.idleHours-a.idleHours});allVehicles=vehicleRows;hydrateVehicleMap();applyFilters();byId('lastUpdatedLabel').textContent='Last updated '+fmtD(new Date());setDebug('Vehicles with usable data: '+vehicleRows.length+'\nQueried vehicles: '+devices.length+'\nCurrent price per liter: '+fuelPricePerLiter().toFixed(3));if(vehicleRows.length){setStatus('Analysis complete. Open any vehicle for a deeper cost and idle review.','The review is ready. Use the filters below to focus on the biggest fuel waste opportunities.',false);refreshResolvedGroups(devices);}else{setStatus('No fuel or idle data was found in the last 30 days.','The Geotab queries returned no usable records for this window.',true);}setLoading(false);},function(e){allVehicles=[];hydrateVehicleMap();applyFilters();setStatus('Could not load trip idle data.','Trip query failed: '+errText(e),true);setDebug('Trip query failed: '+errText(e));setLoading(false);},[]);},function(e){allVehicles=[];hydrateVehicleMap();applyFilters();setStatus('Could not load fuel data.','FuelUsed query failed: '+errText(e),true);setDebug('FuelUsed query failed: '+errText(e));setLoading(false);},[]);},function(e){allVehicles=[];hydrateVehicleMap();applyFilters();setStatus('Could not load vehicles from Geotab.',errText(e),true);setDebug('Device lookup failed: '+errText(e));setLoading(false);});}
  return{init:function(a){api=a;byId('refreshButton').onclick=run;byId('retryButton').onclick=run;byId('exportButton').onclick=exportCsv;byId('rulesButton').onclick=function(){var panel=byId('rulesPanel');panel.className=panel.className.indexOf('hidden')!==-1?'rulesPanel':'rulesPanel hidden';};byId('groupFilter').onchange=applyFilters;byId('statusFilter').onchange=applyFilters;byId('searchFilter').oninput=applyFilters;byId('applySettingsButton').onclick=function(){saveSettings();recomputeAllVehicles();};byId('vehicleTableBody').onclick=handleReviewClick;byId('closeReviewButton').onclick=closeReview;byId('reviewModal').onclick=function(e){if(e.target===byId('reviewModal'))closeReview();};document.addEventListener('keydown',function(e){if(e.key==='Escape')closeReview();});loadSettings();buildGroupOptions([]);renderFleetSummary([]);renderTrendChart([],'fleetTrendChart');setStatus('Ready to analyze fuel waste and idle cost.','Adjust the pricing assumptions if needed, then refresh the dashboard to pull the latest 30-day view.',false);},focus:function(a){api=a;if(!loaded){loaded=true;run();}},blur:function(){}};}());
geotab.addin=geotab.addin||{};
geotab.addin.fuelWasteIdleCost=function(){return{initialize:function(api,state,cb){fuelWasteDashboard.init(api);if(typeof cb==='function')cb();},focus:function(api){fuelWasteDashboard.focus(api);},blur:function(){}};};
